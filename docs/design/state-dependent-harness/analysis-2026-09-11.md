# State-Dependent Harness — Manual Analysis 2026-09-11

Status: PARTIAL
Phase 1 (S0+): PASS
Phase 2 (pair enumeration): PASS
Phase 3 (traversal construction): BLOCKED
Reason: deterministic choice rule for manual traversal not defined

Anchor: `49a5c86be320cab5f998001182e6a3ffa836ec1c`
Root: `2`
Manual budget: `20` substitutions

## 1. S0+

`S0+` is the state after the first `closure()` call on the root-2 initial state.

The nine canonical objects are:

1. `p^2 = 2*q^2`
2. `prime(2)`
3. `integer(p)`
4. `integer(q)`
5. `2 > 1`
6. `gcd(p,q,1)`
7. `2 | p`
8. `p = 2*k_6`
9. `integer(k_6)`

The first six objects are inserted by `State.__init__`. Closure derives `2 | p`, then the divisibility witness `p = 2*k_6`, and then `integer(k_6)`. Canonicalization is applied on insertion.

Phase 1: PASS.

## 2. Source equalities and targets

The only source equality in `S0+` is:

`p = 2*k_6`

The complete target set containing `p` is:

| Source | Target |
|---|---|
| `p = 2*k_6` | `p^2 = 2*q^2` |
| `p = 2*k_6` | `integer(p)` |
| `p = 2*k_6` | `gcd(p,q,1)` |

No other equality has a variable as its left-hand side in `S0+`.

Phase 2: PASS.

## 3. Candidate gate G

The proposed gate is:

`G := 2*k_6^2 = q^2`

For the core target `p^2 = 2*q^2`, substituting `p -> 2*k_6` produces `(2*k_6)^2 = 2*q^2`. The expression canonicalizer rewrites this form to `2*k_6^2 = q^2`.

Therefore a traversal whose first selected pair is

`p = 2*k_6 -> p^2 = 2*q^2`

reaches `G` at step 1.

The alternative first move

`p = 2*k_6 -> integer(p)`

does not remove or modify the source equality `p = 2*k_6` and does not modify the target `p^2 = 2*q^2`. The latter pair remains available. Consequently a traversal taking the predicate target first can take the core equality target next and reaches `G` at step 2.

Thus:

`k_A(G) = 1`

`k_B(G) = 2`

and `G` is not a binary gate within the fixed budget of 20 substitutions.

Verdict: `G REJECTED`.

## 4. Descent candidate

The proposed deeper candidate is:

`k_6^2 = 2*m^2`

Its proposed construction requires a source equality of the form:

`q = 2*m`

The current closure mechanism does not generate a source equality with `q` on the left. The root-2 closure sequence generates divisibility of `p`, the witness equality `p = 2*k_6`, and its integer witness; the later canonical equality `2*k_6^2 = q^2` does not introduce a source equality for `q`.

Therefore the proposed descent candidate is unreachable in the current operator set.

Verdict: `UNREACHABLE`.

## 5. Structural observation

Any traversal that contains the pair

`p = 2*k_6 -> p^2 = 2*q^2`

reaches `G` immediately at that pair. A traversal that first takes either of the other two targets may defer that pair, but the first predicate-target move does not consume the source binding or the core target. Therefore the gate is reached after at most one such neutral move.

`G` cannot separate strategies whose only difference is target ordering over the three initial substitution pairs.

## 6. Traversal construction status

Phase 3 is not closed.

The previously proposed selection criterion — choose the pair that is "most different" from the other traversal — has no deterministic formal definition. Continuing beyond the first discriminating choice would therefore introduce an unspecified choice function.

No `T_A` or `T_B` beyond the mechanically established first-step alternatives is frozen in this document.

No post-hoc RANDOM/TARGETED trace is used to construct either traversal.

## 7. Scope

This document records only the manually established root-2 facts above. It does not freeze a state-dependent harness, exposure mechanism, positive-control traversal, or deterministic TARGETED strategy.