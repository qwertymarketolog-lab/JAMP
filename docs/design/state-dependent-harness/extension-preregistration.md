# Extension Pre-Registration

**Date:** 2026-09-11  
**Status:** INVALIDATED — CREATED PREMATURELY  

This artifact must not be used as a preregistration. It was created before the frozen extension-choice criterion had been formally applied to E1/E2/E3. That ordering violates the required design protocol.

No extension is selected by this artifact.

The file is retained as an explicit audit record rather than silently deleting the premature artifact. A valid `extension-preregistration.md` may be created only after:

1. the frozen criterion exists;
2. E1/E2/E3 have been evaluated against it without implementation results;
3. the criterion yields an admissible unique selection;
4. the selected extension's paper prediction and positive control are fully specified.

Until then:

`SELECTED_EXTENSION = UNDEFINED`

`IMPLEMENTATION = NOT AUTHORIZED`

`EMPIRICAL EXECUTION = NOT AUTHORIZED`
