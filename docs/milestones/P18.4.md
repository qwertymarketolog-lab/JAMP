# P18.4 — Causal Necessity / Ablation

**Status: 🔴 PENDING VERIFIED CI**

## Exact target

Target implementation SHA:

`7b717a8dea269bc5c33d9c128634f55c0bad27b0`

Production guardrail:

`src/jamp/p18/controlled_transfer.py` — locked.

Test harness:

`tests/test_p18_4_ablation.py` — frozen before the authorized test-only correction.

## Original target telemetry

CI Run #165

Run ID: `34212659566`

Branch: `feature/p18-4-ablation-target`

Head SHA: `7b717a8dea269bc5c33d9c128634f55c0bad27b0`

Result: **139 passed / 1 failed**.

The failure was a test-contract collision, not evidence of a production guardrail defect. The fixture used `source_pattern.family_id = family-a` while `authorization.source_family = family-c`. The production validator therefore rejected ownership first, before the intended authorization PermissionError branch could be reached.

## Authorized correction

Only the test fixture may be corrected:

- align `authorization.source_family` with the source pattern ownership (`family-a`);
- retain the foreign/unauthorized authority condition so the authorization guard is actually exercised;
- do not change production code;
- push the corrected test harness and obtain fresh CI telemetry.

## Required GREEN gate

P18.4 can be marked GREEN only after all four observations are present in CI:

1. baseline guardrails enabled and full suite passes;
2. authorization OFF exposes an unauthorized transfer breach;
3. isolation OFF exposes a shared mutable-reference breach;
4. digest OFF exposes acceptance of tampered source material.

Until then, the status remains RED/PENDING regardless of local test results.
