# EXP-10 / R0.4 — ExecutionResult v0

Status: implementation in `exp-10-r0-4-execution-result`.

## Contract

`ExecutionResultV0` contains only:

- `execution_start_hash: Hash256`
- `result_ref: Hash256`

The required causal transition is `EXECUTION_START -> EXECUTION_RESULT`.

Rejected operations must preserve ledger state under L0.
