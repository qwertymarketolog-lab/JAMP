# G1 PR→Run head SHA binding adversarial test

Temporary test only. No production changes.

Second checkpoint: SHA B must produce a new Research Loop v0 Gate run whose authoritative `run.head_sha` equals this PR HEAD.
